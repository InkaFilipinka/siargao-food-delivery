# Supabase (optional - for order persistence)
# Create a project at https://supabase.com, then run supabase/schema.sql in the SQL editor
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Staff auth (optional - when set, GET /api/orders requires this token)
# STAFF_TOKEN=your-secret-token

# JWT for restaurant/driver login (falls back to SUPABASE_SERVICE_ROLE_KEY if unset)
# JWT_SECRET=your-jwt-secret

# EmailJS (optional - for customer order receipt)
# Create account at emailjs.com, set up service + template with vars: to_email, customer_name, order_id, items, subtotal, delivery_fee, tip, priority_fee, total, landmark, address, time_window
# NEXT_PUBLIC_EMAILJS_SERVICE_ID=your_service_id
# NEXT_PUBLIC_EMAILJS_TEMPLATE_ID=your_template_id
# NEXT_PUBLIC_EMAILJS_PUBLIC_KEY=your_public_key

# ntfy (optional - for restaurant push notifications)
# Default: ntfy.sh. Use NTFY_BASE_URL for self-hosted (e.g. https://ntfy.yourdomain.com)
# NTFY_BASE_URL=https://ntfy.sh
